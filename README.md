# task2
# Semantic Profile Page with CSS Styling

This project extends a semantic HTML profile page by applying CSS box model concepts.
Spacing, layout, and visual hierarchy are implemented using margins, padding, and borders.
